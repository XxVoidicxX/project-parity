[project]
name = "project-parity-py"
version = "1.7.0"
requires-python = ">=3.10"
[project.scripts]
parity = "parity_py.cli:main"
parity-doctor = "parity_py.doctor:main"
[tool.setuptools.packages.find]
where = ["."]
